
@model IEnumerable<PremisesTest.Models.Building>
@{
    ViewData["Title"] = "Список зданий";
    Layout = null;
}
<head>
    <title>jqGrid</title>
    <link rel="stylesheet" href="~/css/kendo.common.min.css">
    <link rel="stylesheet" href="~/css/kendo.default.min.css">
    <script src="~/js/jquery.min.js"></script>
    <script src="~/js/kendo/kendo.all.min.js"></script>
    <script src="~/js/kendo/kendo.grid.min.js"></script>
    <script src="~/js/kendo/gridLoad.js"></script>
    <script src="~/js/kendo/gridRoom.js"></script>
</head>
<body>
    @*<div class="form-group">
        <label for="grid">Здания</label>
        <div id="grid"></div>
        @*<div id="gridRoom"></div>
    </div>*@

        <div class="form-group">
            <label for="grid">Список помещений / List buildings</label>
            <div id="grid"></div>
            <br />
            @*<label id="lbGridURoles" style="display: none" for="gridURoles">Роли пользевателя / List roles to user</label>*@
            @*<div id="gridRoom"></div>*@
        </div>
</body>
@*<table class="table">
            <tr>
                <th>@Html.DisplayNameFor(model => model.buildingId)</th>
                <th>@Html.DisplayNameFor(model => model.nameBuilding)</th>
            </tr>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.buildingId)</td>
                    <td>@Html.DisplayFor(modelItem => item.nameBuilding)</td>
                </tr>
            }
    </table>*@
